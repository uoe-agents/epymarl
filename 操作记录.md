# Stage 1 操作记录：LBF 上的 Uncertainty-Aware Teammate Belief-MAPPO

## 最后更新：2026-09-05


## 1. 项目与路径

| 项目 | 值 |
|------|-----|
| 研究目标 | Uncertainty-Aware Teammate Belief-MAPPO（阶段一：LBF 验证） |
| 本地路径 | `D:\codex\epymarl` |
| 服务器路径 | `/home/student/zzq/test/mn/epymarl` |
| 算法框架 | EPyMARL（自带 MAPPO / oracle_mappo） |
| 环境 | LBF (lb-foraging) |
| 硬件 | 服务器 Pod：16 CPU / 128G 内存 / 1 A100 GPU（实际未用 GPU，use_cuda 自动 OFF） |
| 状态 | **Oracle-MAPPO 与 time_limit 已修复，并已在服务器完成验证** |

---

## 2. 已完成事项

### 2.1 环境与框架确认
- [x] 确认 EPyMARL 原生支持 MAPPO（`src/config/algs/mappo.yaml` + `src/learners/ppo_learner.py`）
- [x] 确认 LBF 环境原生注册（`env_requirements.txt` 含 `lbforaging`）
- [x] 确认 LBF 环境注册名列表（预注册了 5x5 ~ 17x17 各规格，sight 有 -1(全局) 和 2 两种）
- [x] 发现 LBF 预注册环境**内置 `max_episode_steps=50`**，`env_args.time_limit` 外层 TimeLimit 无法覆盖
- [x] 发现 Sacred 配置系统限制：命令行不能添加 YAML 未定义的参数（`ConfigAddedError`）
- [x] 确认训练流程可跑通（episode/return/时间步的计数关系）

### 2.2 Oracle-MAPPO 实现
- [x] 在 actor 输入中加入全局 state（所有队友信息）
- [x] 新增 `obs_global_state` 配置开关
- [x] 新建 `oracle_mappo.yaml` 算法配置

---

## 3. 修改的文件

### 3.1 `src/config/default.yaml`
新增配置项：
```yaml
obs_global_state: False # Include global state in actor observation (for Oracle-MAPPO)
```

### 3.2 `src/controllers/basic_controller.py`
`_build_inputs`：当 `obs_global_state=True` 时，将全局 state 广播给所有 agent：
```python
# Oracle-MAPPO: add global state to actor input
if getattr(self.args, "obs_global_state", False):
    state = batch["state"][:, t]  # (bs, state_shape)
    state = state.unsqueeze(1).expand(-1, self.n_agents, -1)  # (bs, n_agents, state_shape)
    inputs.append(state)
```
`_get_input_shape`：对应增加 `scheme["state"]["vshape"]`。

当前实现使用独立的 `global_state` 字段：中央 critic 继续读取历史 `state`，Oracle actor 读取 `global_state`。Oracle actor 初始化时先构造同维度 Local actor，再复制局部参数并将新增 global 列置零，保证两者初始策略逐参数一致。

### 3.3 `src/config/algs/oracle_mappo.yaml`
新建文件，为 mappo.yaml 的副本，唯一差异：
```yaml
obs_global_state: True
name: "oracle_mappo"
```

### 3.4 `src/envs/gymma.py`（TimeLimit 解包 + 基础环境 patch — 真正生效的方案）

**问题**：lb-foraging 预注册环境把 `max_episode_steps=50` 写入 spec.kwargs，导致：
1. `gym.make()` 自动套内层 TimeLimit(50)
2. 基础 ForagingEnv 内部也有 `_max_episode_steps=50` 属性控制步数
3. EPyMARL 外层 `env_args.time_limit=100/150` 永远不会触发（内层先截断 episode）

**尝试过的弯路**：
- 写 `register_relaxed.py` 把源 env 复制为 "Relaxed" 版本（去掉内层步数）。失败：`gym.spec()` 不会触发 gymnasium 的第三方环境懒加载（只有 `gym.make()` 才会），于是查源 env 时它还没注册，Relaxed 版永远注册不出来。
- 只剥 TimeLimit wrapper：失败。`gym.make()` 还会包 `PassiveEnvChecker`、`OrderEnforcing` 等 wrapper，只剥 TimeLimit 后拿到的是 `OrderEnforcing`，不是真正的 `ForagingEnv`，patch `_max_episode_steps` 无效。
- `default.yaml` 的 `max_episode_steps: null` 漏进 `**kwargs` 传给 `gym.make()`，导致使用 spec 默认值 50。

**最终方案**（2026-09-04 验证生效）：三步修复
```python
def __init__(
    self,
    key,
    time_limit,
    pretrained_wrapper,
    seed,
    common_reward,
    reward_scalarisation,
    max_episode_steps=None,  # 关键修复 1: 拦截 default.yaml 的 null，避免漏进 kwargs
    **kwargs,
):
    # 关键修复 2: 显式传 max_episode_steps=time_limit 给 gym.make，覆盖 spec 默认值
    self._env = gym.make(f"{key}", max_episode_steps=time_limit, **kwargs)

    # 关键修复 3: 用 unwrapped 直接跳到最底层的基础环境，剥掉所有 gymnasium wrapper
    # (TimeLimit, PassiveEnvChecker, OrderEnforcing 等)
    base_env = self._env.unwrapped

    # 关键修复 4: patch 基础 ForagingEnv 的内部步数限制属性
    # lb-foraging 的 ForagingEnv 用 _max_episode_steps 控制内部步数
    for attr in ["_max_episode_steps", "_max_steps", "max_steps", "_step_limit"]:
        if hasattr(base_env, attr):
            setattr(base_env, attr, time_limit)
            print(f"[DEBUG GymmaWrapper] Patched {attr}: {old_val} -> {time_limit}")
            break

    # 重新包 TimeLimit，以 time_limit 为准
    self._env = TimeLimit(base_env, max_episode_steps=time_limit)
    self._env = FlattenObservation(self._env)
```

**验证日志**：
```
[DEBUG GymmaWrapper] Patched _max_episode_steps: 50 -> 150
ep_length_mean:          150.0000  # ✅ 从 50 变成 150，time_limit 生效
```

**关键点**：
- `max_episode_steps=None` 作为命名参数被拦截，不进 `**kwargs`
- `gym.make(..., max_episode_steps=time_limit)` 覆盖 spec 注册的默认值
- `unwrapped` 直接跳到 ForagingEnv，绕过所有中间 wrapper
- ForagingEnv 的 `_max_episode_steps` 属性被 patch 为 `time_limit`
- 外层 TimeLimit 也用 `time_limit`

所有 LBF 环境通用，不需要新 id。

`get_state()` 保持原有的局部观测拼接（10x10-3p-3f 为 54 维），供中央 critic 使用；`get_global_state()` 通过独立字段提供 Oracle actor 输入。LBF 的全局输入直接读取基础环境的完整 `field` 和 `players`，按“全部食物 `(row, col, level)` + 全部玩家 `(row, col, level)`”编码。10x10-3p-3f 的全局输入为 18 维，即使三个 agent 当前都看不到食物，Oracle 仍能获得全部食物的真实位置和等级。

---

## 4. 训练命令记录

### 4.1 快速验证跑通（100k steps）
```bash
python src/main.py --config=mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-15x15-4p-4f-v3" \
  env_args.time_limit=100 \
  t_max=100000 \
  seed=0
```

### 4.2 中等难度（15x15-4p-4f，2M steps）
```bash
python src/main.py --config=mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-15x15-4p-4f-v3" \
  env_args.time_limit=100 \
  t_max=2000000 \
  seed=0
```
**结果：失败**

### 4.3 高回报环境（约 10x10-3p-3f-2s，5M steps，Local-MAPPO baseline）
```bash
python src/main.py --config=mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-10x10-3p-3f-v3" \
  env_args.time_limit=50 \
  t_max=5000000 \
  seed=0
```
**结果：收敛良好**

### 4.4 Oracle-MAPPO（5M steps）
```bash
python src/main.py --config=oracle_mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-10x10-3p-3f-v3" \
  env_args.time_limit=50 \
  t_max=5000000 \
  seed=0
```
**结果：收敛良好**

---

## 5. 训练结果汇总

### 5.1 15x15-4p-4f（Local-MAPPO，2M steps）—— 任务过难
| 指标 | 值 |
|------|-----|
| test_return_mean | ~0.15（且训练中从 0.33 退化） |
| test_ep_length_mean | 50.0（跑满限制） |
| 结论 | 该配置不适合作为主实验（训练不稳、return 低） |

### 5.2 强制合作 15x15-4p-4f（Local-MAPPO，2M steps）—— 任务过难
| 指标 | 值 |
|------|-----|
| test_return_mean | ~0.006-0.008（全程贴近 0） |
| test_ep_length_mean | 50.0（100% 超时） |
| critic_loss | 回落至 0.08（critic 已"接受现实"，预测≈0） |
| 结论 | Local-MAPPO 在 sight=2 + force_coop 下完全学不到信号 |
| significance | 为"队友信息价值"提供了最强的极端案例：Local 拿 0，若 Oracle 能学会则 headroom 为几何级 |

### 5.3 约 10x10-3p-3f-2s（5M steps）—— Headroom 对比
| 指标 | Local-MAPPO | Oracle-MAPPO |
|------|------------|--------------|
| test_return_mean | ~0.76-0.78 | ~0.80-0.83 |
| test__return_std | ~0.32 | ~0.30 |
| test_ep_length_mean | ~37 | ~36 |
| 训练时长 | ~69 分钟 | ~72 分钟 |
| 结论 | —— | 差距仅 2-5% |

### 5.4 Headroom 判定
- **验收标准（STAGE1_PLAN）**：Oracle 至少比 Local 高 **10%**
- **10x10-3p-3f-2s 实际差距**：**2-5%，不达标**
- **分析**：环境对 MAPPO 已足够简单，sight=2 约束不够强，50 步限制偏宽松，双方都收敛到接近 0.8，信息上界带来的增益被压缩。
- **coop 15x15-4p-4f 的 Local 结果**：0（完全学不动），等待 Oracle 对比结果。若 Oracle > 0.3，则 headroom 成立且为几何级。

---

## 6. 关键发现（踩坑记录）

1. **LBF 内置 50 步限制（2026-09-04 已解决）**：
   - **问题**：`max_episode_steps=50` 硬编码在环境注册 kwargs 中，`env_args.time_limit` 外层 wrapper 无法覆盖
   - **根因**：
     1. `gym.make()` 根据 spec 自动套 TimeLimit(50)
     2. 基础 ForagingEnv 内部有 `_max_episode_steps=50` 属性控制步数
     3. `default.yaml` 的 `max_episode_steps: null` 漏进 `**kwargs`，导致 `gym.make()` 使用 spec 默认值
     4. 只剥 TimeLimit wrapper 不够，`gym.make()` 还包了 `PassiveEnvChecker`、`OrderEnforcing` 等 wrapper
   - **解决**：
     1. 拦截 `max_episode_steps` 参数，避免漏进 `gym.make()` kwargs
     2. 显式传 `max_episode_steps=time_limit` 给 `gym.make()`
     3. 用 `unwrapped` 直接跳到 ForagingEnv，绕过所有中间 wrapper
     4. Patch ForagingEnv 的 `_max_episode_steps` 属性为 `time_limit`
   - **验证**：`time_limit=150` 时，`ep_length_mean` 从 50 变成 150 ✅
2. **Sacred 严格配置**：命令行参数必须先在 YAML 中定义，否则报 `ConfigAddedError`。
3. **环境选择的两难**：
   - 15x15-4p-4f：任务过难，return < 0.2，Local 本身就学不好
   - 简单环境：好训练但 headroom 小
   - 需要找到"Local 能到 ~0.5-0.7 且 Oracle 能明显更高"的中间难度
4. **GPU 不可用**：服务器 Pod 声明了 A100 但 `use_cuda` 被自动关闭（无 CUDA device），实际在 CPU 训练，速度约 70k steps/分钟。
5. **`gym.spec()` 与 `gym.make()` 的懒加载差异**：尝试用 gym.spec 复制 LBF 环境注册时，源环境尚未被注册（gymnasium 第三方环境的 plugin 懒加载只在 gym.make 时触发），导致新 id 永远注册不出来。结论：不要依赖 spec 复制注册，直接剥掉 TimeLimit 最稳。
6. **Oracle 初始策略初始化错误（2026-09-05 已解决）**：
   - 旧代码用 critic 的 `state_shape` 计算需要清零的 global 列；当 actor 使用独立 global state 时维度不一致，只清零了部分列。
   - 直接重新采样 expanded Linear 会推进 RNG 序列，使 Oracle 与 Local 后续 critic 的初始化不同。
   - 修复：增加 `global_state_shape`，先实例化 Local actor，复制 `fc1/rnn/fc2` 参数到 Oracle actor 的局部部分，将全部 global 列置零，并恢复构造 expanded actor 前的 PyTorch RNG 状态。
   - 验证：Oracle `fc1` global 列最大绝对值为 0，局部参数与 Local actor 最大差值为 0，构造 actor 后的 RNG 状态也与同 seed Local-MAPPO 一致。
7. **Oracle 全局状态语义错误（2026-09-05 已解决）**：
   - 旧实现把三个 sight=2 的局部观测拼接成 54 维输入；若食物不在任何 agent 的视野内，这 54 维中仍完全没有食物信息，因此不是 Oracle。
   - 修复：LBF Oracle 改为读取基础环境完整 `field` 与 `players`，使用固定长度实体向量；critic 仍使用历史 54 维 state，保证 Local/Oracle critic 输入一致。
   - 目标环境的新启动日志应为 `Oracle actor input: obs=18 + agent_id=3 + global_state=18 => 39`；旧日志中的 `75` 表示仍在使用伪全局状态。
8. **目标 coop 环境的奖励稀疏性（2026-09-05 复核）**：
   - 本地均匀随机策略连续 20,000 个 episode（100 万 environment steps）正奖励次数为 0。
   - 服务器旧 Oracle 日志截至 550k steps / 11,000 episodes 仍为 0，原记录中“500k 时 test_return≈0.0267”的说法没有日志支持，已撤回。
   - 操作记录中收敛到 0.8 的 10x10 命令使用的是 `...3f-v3`（非 coop）；不能用于证明 `...3f-coop-v3` 的 MAPPO 在同配置下能学习。

---

## 7. 当前进度

```
环境确认 (完成) → 流程跑通 (完成) → Local baseline (完成) → Oracle-MAPPO (完成)
→ Headroom 验证 (❌ 不足 10%) → time_limit 修复 (✅ 完成) → 放宽时间预算验证 (进行中)
→ GRU belief → delay/policy switch → 多 seed → SMACv2
```

**当前阻塞点**：无。time_limit 已修复，可以执行 coop 15x15 + time_limit=150 的 headroom 验证（见 8.1b）。

---

## 8. 后续计划

### 8.1 当前主线：coop 环境的 Oracle 决定性测试（已出结果：双失败）
```bash
# Local：~0.03 / Oracle：~0.006（均 2M steps, 50 步上限）
```
**判定：coop 15x15 + sight=2 + 50 步上限 超出 Oracle 能力上限（时间预算物理不足）。**
→ 改走 register_relaxed：松开内层 50 步，用 time_limit 当难度旋钮。

### 8.1b 立即执行：coop 15x15 时间预算放宽（time_limit=150，2M steps 起）
gymma.py 已修复（2026-09-04 验证生效）：
- 拦截 `max_episode_steps` 参数，避免漏进 `gym.make()` kwargs
- 用 `unwrapped` 直接跳到 ForagingEnv，绕过所有中间 wrapper
- Patch ForagingEnv 的 `_max_episode_steps` 属性为 `time_limit`

直接用 **stock key + 更大 time_limit** 即可。
```bash
# Oracle 先跑（约 40-50 分钟，150 步 episode 比 50 步慢些）
python src/main.py --config=oracle_mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-15x15-4p-4f-coop-v3" \
  env_args.time_limit=150 \
  t_max=2000000 \
  seed=0

  python src/main.py --config=oracle_mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-10x10-3p-3f-coop-v3" \
  env_args.time_limit=150 \
  t_max=2000000 \
  seed=0


# Local 对比
python src/main.py --config=mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-15x15-4p-4f-coop-v3" \
  env_args.time_limit=150 \
  t_max=2000000 \
  seed=0
# 判读：Oracle > Local 且差 ≥10% → headroom 成立，补跑 5M 正式；都不行 → 调 time_limit（120/200）或降到 10x10 coop
```

### 8.2 Oracle 也学不动的降档备选（10x10 coop）
```bash
python src/main.py --config=oracle_mappo --env-config=gymma with \
  env_args.key="lbforaging:Foraging-2s-10x10-3p-3f-coop-v3" \
  env_args.time_limit=50 \
  t_max=2000000 \
  seed=0
# 若此档 Oracle > 0.3 而 Local < 0.1，headroom 同样成立
```

### 8.3 备选：自定义环境（提高难度）
如果需要更细粒度控制，注册自定义环境：
```python
import gymnasium as gym
from gymnasium.envs.registration import register

register(
    id="Foraging-Hard-v0",
    entry_point="lbforaging.foraging:ForagingEnv",
    kwargs={
        "players": 4,
        "field_size": (15, 15),
        "max_num_food": 6,        # 更多食物
        "sight": 2,
        "max_episode_steps": 30,  # 更短时间（覆盖内置 50 步）
        "force_coop": True,       # 强制合作（需要多智能体才能采集）
        "max_player_level": 3,
    },
)
```
然后用 `python -c "import register_custom_lbf; exec(open('src/main.py').read())" ...` 或 `sys.path` 方式导入。

### 8.4 其他备选难度调节
- 减少 `max_episode_steps` 到 30-40（压迫效率）
- 使用 `force_coop=True` 版本（检查 `Foraging-*-coop-v3` 是否已注册）
- 增大地图到 16x16 / 17x17
- agent 数从 4 增到 5-6

### 8.5 如果 headroom 始终不足
- 调整论文定位：从"改进最终性能"改为"改进样本效率"（Oracle 前期学得快也有价值）
- 或更换环境：SMACv2（1c3s5z）

### 8.6 Headroom 达标后（进入 GRU belief）
1. 实现 Deterministic-GRU belief-MAPPO（每队友独立循环隐状态 GRU）
2. 对比 last-value / constant-velocity baseline
3. 实现 Uncertainty-GRU（mu/sigma/Mahalanobis/融合公式）
4. 加入 delay-1 / policy-switch 扰动
5. 3 seeds 筛选 → 5 seeds 确认

---

## 9. 下次对话第一条指令

```
请读取 D:\codex\epymarl\操作记录.md，
先跑 Foraging-2s-15x15-4p-4f-v3 的 Local vs Oracle headroom 对比（各 1 个 seed, 2M steps 起步），
确认 headroom 是否扩大，若不足 10% 再注册自定义更难的 LBF 环境。
```

## 10. 2026-09-05 服务器复核补充

- 图中 Oracle 曲线为旧版运行结果；该运行使用的 Sacred source `run.py` 没有独立的 `global_state` scheme，不能代表当前修复后的代码。
- 当前版本在本地 `D:\Anaconda\envs\pytorch` 环境复现同一目标环境：20k steps 的短跑不能稳定采到正奖励；本地均匀随机策略连续 20,000 个 episode 也全部为 0。这个结果确认该 coop 配置奖励极稀疏，不能用短跑 return=0 判断 actor 链路损坏。
- LOAD 动作可用，环境不会吞掉动作；服务器日志中的 `load_actions_mean` 也证明策略确实在选择 LOAD。
- 新增运行诊断指标：`load_actions_mean`（每 episode 选择 LOAD 的次数）、`positive_reward_steps_mean`（每 episode 收到正奖励的 transition 数）、`oracle_global_grad_norm`（Oracle global 输入列梯度范数），并在 Oracle 启动时打印 actor 输入维度。服务器复核时先看这四项，避免把“尚未探索到食物”误判成代码故障。
- 本次会话第一次 SSH 探测未通过；随后使用密码认证成功连接，并且所有服务器操作都限制在 `/home/student/zzq/test/mn/epymarl` 内。

## 11. 2026-09-05 第二次服务器复核

- 服务器 run 18 已加载独立 `global_state` 链路：旧输入日志为 `18 + 3 + 54 = 75`，且 `oracle_global_grad_norm > 0`、`load_actions_mean > 0`，说明数据传递、反向传播和 LOAD 动作均正常。
- 但 54 维来自局部观测拼接，不包含视野外食物。seed=0 的初始地图检查中，三个局部观测看到的食物数为 0，而真实地图上有 3 个食物；因此该版本不是有效 Oracle。
- 本地已将 LBF `global_state` 修正为完整实体状态，目标环境维度为 18，真实状态检查通过；上传后启动日志必须显示 `18 + 3 + 18 = 39`。
- 服务器 run 20（修复后 200k 短跑）启动日志为 `18 + 3 + 18 = 39`，100k 首次出现正奖励，200k `return_mean=0.0007`、`oracle_global_grad_norm=0.0275`。
- 服务器 run 22（修复后同 seed、同配置 2M）已完成：`return_mean` 首次在 60.5k 为正，最终为 `0.6175`；`test_return_mean` 首次在 100.5k 为正，最终 1.90M 评估为 `0.6467`，最大 `0.6833`。这与同环境历史 MAPPO 2M 的 `test_return_mean=0.6733` 已处于同一量级，说明 Oracle 不再是 return 恒为 0 的失败分支。
