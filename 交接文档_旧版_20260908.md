# Switching-LBF 项目交接文档

最后更新：2026-09-08

## 1. 项目目标与路径

研究目标：在 LBF 中验证队友策略切换场景下的 Local-MAPPO、Type-Oracle、Last-action 和 Deterministic-GRU Belief-MAPPO，并进一步研究不确定性建模。

本地项目：D:\codex\epymarl

服务器项目：/home/student/zzq/test/mn/epymarl

服务器上的所有修改和运行都必须限定在 EPyMARL 项目目录内，不要记录或提交服务器密码。

## 2. 当前阶段结论

当前已经完成：

- Switching-LBF 环境实现并通过 1000 episode 有效性检查。
- Local-MAPPO、Type-Oracle-MAPPO、Last-action-MAPPO、Deterministic-GRU Belief-MAPPO 训练完成。
- 每种方法已经有 seed 0、1、2 的正式训练结果。
- 四种方法的 checkpoint 已保存，并完成了固定切换条件下的 12 次评估。
- 有效的固定切换评估日志位于 results/checkpoint_eval_fixed/。

当前还没有完成：

- 切换后逐步恢复速度评估。
- Uncertainty-GRU / belief entropy 等最终创新模块。
- 5 个 seed 的最终确认实验。

当前最准确的结论是：

> Switching-LBF 已经能够产生有效的队友策略差异。Type-Oracle 和 Belief-GRU 均优于 Local-MAPPO，但 Belief-GRU 目前尚未超过 Last-action 基线。下一步应测量切换后的恢复速度，而不是继续盲目增加训练步数。

## 3. 已实现的代码

### 3.1 Switching-LBF 环境

文件：src/envs/switching_lbf.py

环境基础：

- lbforaging:Foraging-2s-10x10-3p-3f-v3
- 3 个 LBF 玩家，其中 1 个 ego 由 MAPPO 控制，2 个由脚本策略控制。
- 队友模式：greedy、left_priority、wait。
- 支持 episode 中途切换队友策略。
- 支持固定模式和固定切换时刻。

可控参数：

~~~yaml
initial_mode_ids: null
switch_mode_ids: null
fixed_switch_step: null
~~~

已注册环境：

~~~text
epymarl/Switching-LBF-v0
epymarl/Switching-LBF-TypeOracle-v0
epymarl/Switching-LBF-LastAction-v0
epymarl/Switching-LBF-Belief-v0
~~~

关键修复：LBF 的 LOAD 要求玩家站在食物相邻格，而不是食物格本身。当前队友目标和固定 ego 检查策略都使用可装载的相邻站位。

### 3.2 四种算法配置

~~~text
src/config/algs/mappo.yaml
src/config/algs/type_oracle_mappo.yaml
src/config/algs/last_action_mappo.yaml
src/config/algs/deterministic_belief_mappo.yaml
~~~

deterministic_belief_mappo 使用共享 GRU 和每个队友独立的 hidden state，当前还不是最终的 uncertainty-aware 版本。

### 3.3 其他相关修改

~~~text
src/controllers/belief_controller.py
src/controllers/basic_controller.py
src/envs/gymma.py
src/config/envs/gymma.yaml
src/runners/parallel_runner.py
src/runners/episode_runner.py
src/run.py
~~~

这些修改涉及 belief controller、Oracle 输入、LBF time limit、切换参数声明、训练诊断指标和模型保存/加载。

## 4. 环境有效性检查

检查脚本：

~~~text
switching_lbf_validity_check.py
~~~

结果文件：

~~~text
results/switching_lbf_validity_1000.csv
~~~

检查规模：6 组条件，每组 1000 episodes。

关键结果：

~~~text
greedy+greedy -> greedy+greedy
before=0.4582, after=0.0011, total=0.4592

greedy+greedy -> left_priority+left_priority
before=0.4625, after=0.0709, total=0.5334

greedy+greedy -> wait+wait
before=0.4580, after=0.0014, total=0.4594

greedy+greedy -> left_priority+wait
before=0.4584, after=0.0403, total=0.4987
~~~

说明：队友模式确实会改变切换后的任务回报，场景不是“所有模式效果一样”。早期全零结果是固定 ego 错误地把食物格当成 LOAD 站位造成的，不能作为最终结论。

## 5. 正式训练结果：约 1.96M steps

这些结果来自 Sacred 的正式训练日志，每种方法取 seed 0、1、2 的最后 5 次测试均值，再在 seed 之间计算均值和样本标准差。

~~~text
Local-MAPPO       0.112827 ± 0.011496
Type-Oracle       0.128345 ± 0.014456
Last-action       0.126662 ± 0.004550
Belief-GRU        0.122285 ± 0.012824
~~~

相对 Local-MAPPO：

~~~text
Type-Oracle       +13.7%
Last-action       +12.3%
Belief-GRU         +8.4%
~~~

Belief-GRU 达到 Type-Oracle 的约 95.3%。

这组结果支持：环境存在约 10% 以上的信息 headroom，Belief-GRU 获得了正向收益。但 Belief-GRU 还没有超过 Last-action，不能宣称当前创新已经优于所有基线。

## 6. Checkpoint 状态

checkpoint 根目录：

~~~text
checkpoints/switching_lbf/
~~~

每种方法都有 seed 0、1、2：

~~~text
local_seed0/       local_seed1/       local_seed2/
oracle_seed0/      oracle_seed1/      oracle_seed2/
last_action_seed0/ last_action_seed1/ last_action_seed2/
belief_seed0/      belief_seed1/      belief_seed2/
~~~

当前选出的最大 checkpoint 约为 1.80M steps：

~~~text
Local:       1802942, 1802482, 1802066
Oracle:      1802652, 1802638, 1802234
Last-action: 1802435, 1803347, 1802510
Belief:      1802586, 1803343, 1803146
~~~

Belief checkpoint 比其他方法多一个：

~~~text
belief_gru.th
~~~

普通方法至少包含：

~~~text
agent.th
critic.th
agent_opt.th
critic_opt.th
~~~

模型加载规则非常重要：

~~~text
checkpoint_path = 数字 checkpoint 目录的父目录
load_step       = 数字 checkpoint 目录名
~~~

例如：

~~~text
checkpoint_path=.../Switching-LBF-Belief-v0_时间戳
load_step=1802586
~~~

不能把 checkpoint_path 直接指向 .../1802586。

四种算法和环境必须严格匹配：

~~~text
Local:
  --config=mappo
  epymarl/Switching-LBF-v0

Oracle:
  --config=type_oracle_mappo
  epymarl/Switching-LBF-TypeOracle-v0

Last-action:
  --config=last_action_mappo
  epymarl/Switching-LBF-LastAction-v0

Belief:
  --config=deterministic_belief_mappo
  epymarl/Switching-LBF-Belief-v0
~~~

Oracle checkpoint 如果使用普通 mappo 和普通 Switching-LBF 环境，会出现输入维度错误：checkpoint 的 fc1.weight 为 [128, 25]，普通模型为 [128, 19]。多出来的 6 维是两个队友的模式 one-hot。

## 7. 固定切换 checkpoint 评估

日志目录：

~~~text
results/checkpoint_eval_fixed/
~~~

12 个日志均已完成，没有 Traceback 或 ValueError。

统一条件：

~~~text
初始模式：greedy + greedy
切换模式：left_priority + left_priority
切换时刻：第 10 步
每个 checkpoint：1000 episodes
~~~

三 seed 汇总：

~~~text
Local-MAPPO       0.1197 ± 0.0145
Type-Oracle       0.1315 ± 0.0061
Last-action       0.1320 ± 0.0108
Belief-GRU        0.1257 ± 0.0182
~~~

相对 Local-MAPPO：

~~~text
Type-Oracle       +9.9%
Last-action      +10.3%
Belief-GRU        +5.0%
~~~

Belief-GRU 达到 Oracle 的约 95.6%，但比 Last-action 低约 4.8%。Belief 的 seed 间波动最大。

这组评估使用约 1.80M checkpoint，不能和约 1.96M 的训练日志直接混为同一组结果；它的用途是验证固定切换条件，而不是重新计算训练曲线。

重要诊断：

~~~text
test_switching_lbf_switched_mean ≈ 1.0
test_switching_lbf_switch_step_mean = 10.0
~~~

说明切换确实发生且时刻正确。

~~~text
test_load_actions_mean = 0
~~~

当前不能使用这个指标。runner 只对环境 key 以 lbforaging: 开头的环境开启 LOAD 统计，而 Switching-LBF 的 key 是 epymarl/Switching-LBF-*。它不代表策略没有执行 LOAD。

## 8. 已遇到的问题与处理方式

### 8.1 早期有效性检查全零

原因：固定策略把食物格当成 LOAD 位置，没有站在食物相邻格。

处理：已修复环境队友目标和 validity check 的 ego 目标。

### 8.2 Sacred 拒绝 fixed_switch_step

原因：参数没有预先声明在 src/config/envs/gymma.yaml。

处理：已声明：

~~~text
initial_mode_ids
switch_mode_ids
fixed_switch_step
~~~

### 8.3 checkpoint 报 min() arg is an empty sequence

原因：checkpoint_path 指向了数字 checkpoint 目录，而不是它的父目录。

处理：使用父目录加 load_step。

### 8.4 Oracle 报 fc1 输入维度不匹配

原因：使用了普通 Local 的 config/env 加载 Oracle checkpoint。

处理：必须使用 type_oracle_mappo 和 Switching-LBF-TypeOracle-v0。

### 8.5 tmux 会话立即退出

服务器上的 tmux shell 启动不稳定。当前已经使用 nohup ... > log 2>&1 < /dev/null & 完成评估，结果保存在 results/checkpoint_eval_fixed/。后续优先使用 nohup，避免依赖 tmux。

### 8.6 旧的 SMAC 进程

旧的 sc2v2 Protoss 进程 PID 49337 已经结束，目前没有残留的 sc2v2 训练进程。不要使用 killall python，避免误杀当前实验。

## 9. 下一步计划：恢复速度评估

### 9.1 第一优先级：增加逐步切换统计

当前日志只有整回合聚合回报，不能回答“切换后恢复用了多少步”。应修改 ParallelRunner，利用每一步返回的 data["info"] 和 reward 记录 episode 内分段指标。

建议记录：

~~~text
pre_switch_return
post_switch_return_10
post_switch_return_20
post_switch_positive_rate
first_positive_after_switch_steps
recovery_to_80_percent_steps
~~~

切换边界应以每一步 info 中第一次出现：

~~~text
switching_lbf_switched == 1
~~~

为准，不要只根据硬编码的 step 数判断。因为环境在 step 调用前切换，返回的 step 编号和人类口径可能相差 1。

稀疏奖励下，first_positive_after_switch_steps 对没有正奖励的 episode 应标记为 censored/未恢复，不要把它当成 0 步。

### 9.2 第二优先级：用现有 checkpoint 重跑评估

第一组保持当前最重要条件：

~~~text
greedy + greedy -> left_priority + left_priority
fixed_switch_step=10
1000 episodes
~~~

然后补充：

~~~text
greedy + greedy -> wait + wait
greedy + greedy -> left_priority + wait
~~~

四种方法和三个 seed 都使用同样的条件，不需要重新训练。

### 9.3 第三优先级：根据恢复结果做决策

如果 Belief-GRU 的切换后恢复速度在至少两个条件下优于 Last-action，即使总回报略低，也可以把论文贡献定位为：

> 面向队友策略切换的快速适应。

此时再实现：

~~~text
GRU mode logits
softmax teammate posterior
belief entropy / uncertainty
根据 uncertainty 融合 belief 和局部观测
~~~

之后补跑 5 seeds 和消融实验。

如果 Belief-GRU 的总回报和恢复速度都不如 Last-action，应先修改 belief 表征或训练目标，不要继续盲目增加训练步数，也不要立即更换场景。

## 10. 常用检查命令

查看固定评估是否完成：

~~~bash
grep -H -E \
  'test_return_mean|test_positive_reward_steps_mean|test_switching_lbf_switched_mean|Traceback|ValueError|pymarl Completed' \
  results/checkpoint_eval_fixed/*.log
~~~

查看运行中的 EPyMARL 进程：

~~~bash
ps -fu zzq | grep '[s]rc/main.py'
~~~

查看 checkpoint 最大步数：

~~~bash
find checkpoints/switching_lbf -type d -name '[0-9]*' -printf '%p\n' | sort -n -kNF/ | tail
~~~

如果需要启动后台评估，使用：

~~~bash
nohup python src/main.py ... > results/checkpoint_eval_fixed/name.log 2>&1 < /dev/null &
~~~

## 11. 交接时的核心提醒

1. 不要把 results/checkpoint_eval/*.log 当作有效结果；那批日志是错误 checkpoint 路径导致的失败。
2. 有效固定切换评估在 results/checkpoint_eval_fixed/。
3. 不要把约 1.80M checkpoint 评估和约 1.96M 训练 metrics 直接混合排名。
4. Oracle、Last-action、Belief 必须使用各自匹配的 config 和环境 ID。
5. test_load_actions_mean=0 当前不可信。
6. 当前最需要的是恢复速度指标，不是继续增加训练步数。

