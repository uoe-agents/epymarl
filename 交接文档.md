# Switching-LBF 当前交接文档

最后更新：2026-09-12

> 本文是新窗口的主入口。历史 v0、Fixed-v1、Intent-v1 已停止作为研究主线。详细逐步证据见 `SWITCHING_LBF_WORKLOG.md`，但下一步操作以本文为准。

## 1. 当前一句话状态

Intent-v2 已完成 Local、Type-Oracle、Last-action、Deterministic Belief 四种方法的 seed 0 训练和全部 12 组固定条件 × 1000 episodes 评估。Belief 平均优于 Last-action，但只在两个真实切换条件中的 right-right 成功，在 wait-wait 失败。

下一步是多 seed 复现与 wait 模式诊断。不要继续修改环境，也不要直接实现 uncertainty。

## 2. 项目位置与 Git

```text
本地：D:\codex\epymarl
服务器：/home/student/zzq/test/mn/epymarl
分支：codex/lbf-switching-prototype
当前已同步提交：4b6f1bd (intentv2)
```

用户此前要求先不要由 Codex 提交代码。后续除非用户明确要求，否则只修改、验证并说明待同步文件，不执行 commit。

服务器命令必须在项目目录内执行；不要记录服务器密码。

## 3. 必须保留的版本边界

### v0：历史复现，已确认有实现缺陷

旧脚本队友把食物格当作移动目标，但 LBF 必须站在食物相邻格 LOAD，因此队友从未 LOAD。轨迹审计为：

```text
100 episodes / 5000 steps
teammate_load_actions=0
adjacent_nonload_decisions=7522
```

旧 v0 checkpoints 和 recovery 数值只用于复现，不能作为最终算法结论。不得修改 v0 注册或用新环境语义加载旧 checkpoint。

### Fixed-v1：动作修复诊断版，已否决

Fixed-v1 修复相邻格 LOAD，但使用非 coop LBF。ego 全程 NOOP 时脚本队友仍能获得团队奖励，因此不适合作为研究任务。

### Intent-v1：强制合作但队友意图冲突，已否决

Intent-v1 使用 coop 环境，但训练时两个队友的模式不放回抽样，初始共享率为 0；需要三人合作时两个队友却经常奔向不同食物。正式 headroom 评估中 Oracle 平均只比 Local 高约 2.1%，且 Last-action 略高于 Oracle。

不得延长 Intent-v1、增加其 seeds 或在其上训练 Belief。

### Intent-v2：当前唯一主线

Intent-v2 新增 `shared_teammate_mode=True`：两名脚本队友共享一个隐藏意图，并在 episode 中共同切换到另一个意图。

```text
base_key=lbforaging:Foraging-2s-10x10-3p-3f-coop-v3
teammate_modes=(left_priority, right_priority, wait)
use_load_positions=True
shared_teammate_mode=True
```

注册 ID：

```text
epymarl/Switching-LBF-Intent-v2
epymarl/Switching-LBF-TypeOracle-Intent-v2
epymarl/Switching-LBF-LastAction-Intent-v2
epymarl/Switching-LBF-Belief-Intent-v2
```

版本门禁：Intent-v1 初始模式共享率 `0.000`；Intent-v2 初始和切换后共享率均为 `1.000`。

## 4. 重要代码修复

### 4.1 正确的 LOAD 站位

`src/envs/switching_lbf.py` 新增相邻合法站位选择、right-priority、队友 LOAD 次数和已采集食物诊断指标。旧版本默认参数保持不变。

### 4.2 evaluate episode 数修复

旧 `evaluate_sequential` 对 ParallelRunner 循环 `test_nepisode` 次，每次又运行 `batch_size_run` 个 episode，导致标称数量扩大十倍。`src/run.py` 已改为按 runner batch size 计算运行次数，并有单元测试。

影响：历史标称 1000 的某些 v0 评估实际为 10000；修复后的 Intent-v1/v2 正式评估数量正确。

### 4.3 版本门禁隔离无关依赖

`scripts/check_switching_lbf_versions.py` 通过文件路径直接加载 `switching_lbf.py`，避免经 `envs/__init__.py` 导入无关的 SMAClite、sklearn、scipy。

## 5. Intent-v2 已完成训练

统一设置：seed 0、500k timesteps、ParallelRunner、每 50k 保存一次、100 个在线测试 episodes。

最新可用 checkpoint：

```text
local        453306
oracle       453189
last_action  453777
belief       452850
```

服务器 checkpoint 根目录：

```text
results/intent_v2_headroom/artifacts/<method>/models/
```

最后在线测试回报：

```text
local        0.1307
oracle       0.1313
last_action  0.1213
belief       0.1353
```

在线指标只作为训练健康检查，最终比较必须使用固定条件配对评估。

## 6. Intent-v2 三基线正式评估

Local、Oracle、Last-action 使用对应最新 checkpoint；初始模式为 left-left，固定第 25 步切换；条件为 same、right-right、wait-wait；每条件 1000 episodes；方法间使用配对 evaluation seed。

9/9 日志完成，无错误。

三条件平均：

```text
method       return    pre       post5     post10    post20    no-positive
local        0.184433  0.121567  0.015367  0.030600  0.054233  0.838267
oracle       0.197200  0.134567  0.013367  0.030533  0.056733  0.839533
last_action  0.182533  0.135433  0.012000  0.024867  0.043533  0.878333
```

结论：

- Oracle 在 3/3 条件中优于 Last-action。
- Oracle 总回报比 Last-action 高约 8.0%。
- Oracle post10 高约 22.8%，post20 高约 30.3%。
- Oracle 在 3/3 条件中也优于 Local，总回报平均高约 6.9%。
- Oracle post5 低于 Local，no-positive 与 Local 基本相同，不能声称所有恢复指标都占优。

以上结果通过了“隐藏类型有价值”和“Last-action 不能完全替代类型”的门禁，因此只允许训练 Deterministic Belief；该训练现已完成。

## 7. 当前唯一下一步：Belief 固定评估

> **状态更新：本节评估已于 2026-09-12 完成，12/12 日志成功。下列命令仅供复现；当前结论和下一步见第 12 节。**

本地最新评估脚本：

```text
scripts/run_switching_intent_v2_checkpoint_eval.sh
SHA256=07d82aa211abfd6bdf4910f8c8dead6629fa1e4c5b5d038cd29818b75a7166bc
```

这是当前包含 Belief 的四方法脚本校验值。新窗口仍应先确认脚本内容包含四种方法；若后续修改过脚本，则重新计算 SHA256，不要机械匹配旧值。

服务器应先处于 `(marl)` 环境：

```bash
cd ~/test/mn/epymarl
conda activate marl

grep -E '^methods=|^configs=|Belief-Intent-v2' \
  scripts/run_switching_intent_v2_checkpoint_eval.sh
```

应看到：

```text
methods=(local oracle last_action belief)
configs=(mappo type_oracle_mappo last_action_mappo deterministic_belief_mappo)
epymarl/Switching-LBF-Belief-Intent-v2
```

然后 dry-run：

```bash
DRY_RUN=1 TEST_NEPISODE=20 \
bash scripts/run_switching_intent_v2_checkpoint_eval.sh \
| tee results/intent_v2_belief_eval_dry_run.txt

grep '^Selected method=' results/intent_v2_belief_eval_dry_run.txt
grep -c '^DRY RUN' results/intent_v2_belief_eval_dry_run.txt
```

预期：已有 Local/Oracle/Last-action 的 9 个日志全部显示 `Skipping completed`；选择 Belief checkpoint `452850`；DRY RUN 数量为 `3`。

正式补跑：

```bash
nohup env TEST_NEPISODE=1000 MAX_JOBS=2 \
bash scripts/run_switching_intent_v2_checkpoint_eval.sh \
> results/intent_v2_checkpoint_eval/belief_supplement.log \
2>&1 < /dev/null &

echo $! | tee results/intent_v2_checkpoint_eval/belief_supplement.pid
```

完成检查：

```bash
grep -l 'pymarl Completed' \
  results/intent_v2_checkpoint_eval/*__*.log | wc -l

grep -H -E 'Traceback|ValueError|ModuleNotFoundError|size mismatch' \
  results/intent_v2_checkpoint_eval/*__*.log

cat results/intent_v2_checkpoint_eval/summary_by_method_condition.csv
```

预期总完成数为 `12`，错误检查无输出。

## 8. Belief 结果的决策规则

> **执行结果：Belief 相对 Last-action 的平均总回报、post10、post20 均提高，但真实切换条件 right-right / wait-wait 为一胜一负。因此方法可行性门禁通过，稳定快速适应结论未通过。不要直接进入 uncertainty。**

正式结果出来前不要实现 uncertainty。

继续条件：Belief 的三条件平均总回报高于 Last-action，并在 post10/post20 或至少两个切换条件中表现出稳定优势。若满足，可把下一阶段定义为先确认 deterministic belief 的多 seed 稳定性，再设计 uncertainty 消融。

停止条件：Belief 与 Last-action 持平或更差，且恢复指标也无优势。此时停止增加模型复杂度，先诊断 belief 表征/监督信号；不要靠延长训练或立即增加 seeds 掩盖问题。

Oracle 是上界参照，不要求 Belief 超过 Oracle。若 Belief 偶然超过 Oracle，只能视为 seed 0 现象，必须多 seed 后才能解释。

## 9. 关键文件

```text
src/envs/switching_lbf.py
src/run.py
src/runners/switching_metrics.py
src/controllers/belief_controller.py
src/config/algs/deterministic_belief_mappo.yaml
scripts/check_switching_lbf_versions.py
scripts/run_switching_intent_v2_headroom.sh
scripts/run_switching_intent_v2_last_action.sh
scripts/run_switching_intent_v2_belief.sh
scripts/run_switching_intent_v2_checkpoint_eval.sh
scripts/summarize_switching_recovery.py
tests/test_switching_lbf_fixed.py
tests/test_switching_metrics.py
tests/test_evaluate_episode_count.py
SWITCHING_LBF_WORKLOG.md
```

## 10. 操作纪律

1. 每次运行前确认提示符是 `(marl)`；曾有一次从 `(base)` 启动评估，6 个日志全部不完整，失败目录已保留。
2. 启动器拒绝覆盖不完整日志。遇到失败先查看并把整个输出目录移动为带时间戳的归档，不要删除。
3. 不混用不同环境版本、config 或 checkpoint 输入维度。
4. 不用在线训练指标替代固定配对评估。
5. 不修改 v0、Fixed-v1、Intent-v1 的语义。
6. 当前框架按间隔保存 checkpoint，不保证在 500k 退出时额外保存最终模型，因此统一使用自动发现的最大数字 checkpoint。
7. Gymnasium reward 类型、PettingZoo/VMAS 缺失、pkg_resources deprecated 等 warning 已确认不是本实验错误。
8. 所有实验结论和异常追加到 `SWITCHING_LBF_WORKLOG.md`，不要覆盖历史结论。

## 11. 新窗口开场建议

向新窗口发送：

```text
请完整阅读交接文档.md 和 SWITCHING_LBF_WORKLOG.md，并从交接文档第 12 节继续。Intent-v2 seed 0 的四方法固定评估已完成。先核验结果文件与 Git 状态，再规划多 seed 复现和 wait 模式诊断；不要提交代码、不要直接实现 uncertainty。
```

## 12. 2026-09-12 最终结果与下一步

Belief checkpoint `452850` 已完成 same、right-right、wait-wait 三个固定条件，每条件 1000 episodes。当前四方法共 12/12 日志完成且无错误。

Belief 三条件平均：

```text
return=0.19047
pre=0.13877
post5=0.01193
post10=0.02973
post20=0.04857
no-positive-after-switch=0.86470
```

关键比较：

- 相对 Last-action：总回报约 `+4.35%`，post10 约 `+19.6%`，post20 约 `+11.6%`。
- 相对 Local：总回报约 `+3.27%`，但 post10 约 `-2.8%`，post20 约 `-10.4%`。
- 相对 Oracle：总回报约 `-3.4%`。
- right-right：Belief 全面改善，return `0.2087`、post10 `0.0380`、post20 `0.0734`、no-positive `0.7869`。
- wait-wait：Belief 明显失败，return `0.1787`、post10 `0.0211`、post20 `0.0279`、no-positive `0.9204`，均弱于 Last-action 的对应恢复表现。

科学结论：历史编码相对单步 Last-action 有平均价值，但“稳定快速适应”尚未成立，因为两个真实切换条件为一胜一负。same 是无真实模式变化的对照，不能算作恢复成功条件。

实现限制：当前 `BeliefMAC` 没有显式 mode posterior、mode logits 或类型监督损失。它是端到端 recurrent latent encoder；actor 直接看到完整 last-action obs，belief GRU 又重复接收动作证据。不能直接对当前 hidden state 计算 entropy 并称为校准 uncertainty。

下一步顺序：

1. 将服务器 `results/intent_v2_checkpoint_eval/` 完整同步回本地并复核 CSV/日志。
2. 为四种方法补 seed 1、2，使用相同训练预算和配对评估；不要只增加 Belief seeds。
3. 同时做只读诊断：按切换类型比较 belief 的 post5/post10/post20、no-positive，并检查 wait 时 GRU hidden state 是否保留旧意图。
4. 只有多 seed 仍稳定优于 Last-action，才设计显式 posterior、类型预测目标和 uncertainty 消融。
5. 若多 seed 优势消失，停止增加模型复杂度；保留 seed 0 为探索性结果，不包装成最终结论。

## 13. 2026-09-12 两阶段多 seed 决策

用户明确要求先验证核心对比，再决定是否补全实验。因此不立即训练四种方法的全部 seeds。

阶段 A（现在执行）：

- 仅训练 Last-action 与 Belief 的 seed 1、2，共 4 个训练。
- 与已有 seed 0 合并，形成两方法各 3 个训练 seeds。
- 固定评估仍运行 same、right-right、wait-wait，每条件 1000 episodes；科学判定只把 right-right 和 wait-wait 计入恢复主指标。

核心门禁在看到新结果前固定为：

1. 每个训练 seed 计算 right-right 与 wait-wait 的平均 post10；Belief 在至少 2/3 seeds 上高于 Last-action。
2. 三 seeds 汇总后，Belief 的真实切换平均 post10 和 post20 都高于 Last-action。
3. wait-wait 的三-seed 汇总 post10 不低于 Last-action。若仍然更差，则只说明 right 模式特化，不能判为稳定适应成功。

阶段 B（仅门禁通过后执行）：

- 补 Local 与 Oracle 的 seed 1、2，共 4 个训练。
- 完成四方法、三训练 seeds 的正式配对比较。

脚本变更：

- 新增 `scripts/run_switching_intent_v2_core_multiseed.sh`，默认并行训练每个 seed 的 Last-action 与 Belief，依次完成 seed 1、2。
- `scripts/run_switching_intent_v2_checkpoint_eval.sh` 新增 `METHODS` 过滤。
- checkpoint 发现逻辑已改为匹配 `_seed${SEED}_`，防止多 seed 后错误加载其他训练 seed 的全局最大 checkpoint。
- seed 0 历史评估不受该缺陷影响，因为当时每个方法目录只有一个训练 seed。
